{
	"name": "browserify-substitution-mass-confusion",
	"version": "1.0.0",
	"description": "Browserify plugin for substituting modules everywhere. It expands the package.json \"browser\" field to work for nested dependencies.",
	"keywords": [
		"browserify-plugin"
	],
	"homepage": "https://github.com/moll/browserify-substitution-mass-confusion",
	"bugs": "https://github.com/moll/browserify-substitution-mass-confusion/issues",

	"author": {
		"name": "Andri Möll",
		"email": "andri@dot.ee",
		"url": "http://themoll.com"
	},

	"repository": {
		"type": "git",
		"url": "git://github.com/moll/browserify-substitution-mass-confusion.git"
	},

	"licenses": [{
		"type": "LAGPL",
		"url": "https://github.com/moll/browserify-substitution-mass-confusion/blob/master/LICENSE"
	}],

	"main": "index.js",
	"scripts": {"test": "make test"},

	"dependencies": {
		"resolve": "^1.4"
	},

	"peerDependencies": {
		"browserify": ">= 14 < 15"
	},

	"devDependencies": {
		"browserify": ">= 14 < 15",
		"mocha": ">= 2 < 4",
		"co-mocha": ">= 1.2 < 2",
		"must": ">= 0.13.0 < 0.14"
	}
}
